## Session Context (Reversing MCP)

You have access to a static binary analysis MCP server with 13 tools for reverse engineering and Directmedia format analysis.

**Before starting work:**
1. Check available tools: check_tools()
2. Analyze a binary: analyze_binary('file.exe', ['static', 'pe', 'strings'])

**At end of work:**
- Use digibib_research_snapshot() for DigiBib/Directmedia prelude
